package com.enterprise.exchange.config;

import org.springframework.context.annotation.Configuration;

/**
 * Redis connectivity is configured through standard spring.redis properties.
 */
@Configuration
public class RedisConfig {
}
